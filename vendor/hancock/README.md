# hancock

`hancock` is a small HTTP Signature helper used by Lotide for ActivityPub
request signing and verification.

The crate is vendored here so Lotide can keep its federation signing behavior
stable while the surrounding HTTP stack is modernized.
